    <div class="container">
      <div class="sticky mt-1 mb-1">
        <div id="map"
          style="width: 375px; height: 50vh;"
          data-markers="<%= @markers.to_json %>"
          data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
        </div>
      </div>
    </div>

<div class="cards">
  <% @spots.each do |spot| %>
<div class="card-trip">
  <%= image_tag(spot.photos[0], style: 'height: 200px; width:auto;') %>
  <div class="card-trip-infos">
    <div>
      <h4><%= link_to spot.name.capitalize, spot %></h4>
      <p><%= spot.description %></p>
    </div>
    <h4 class="card-trip-pricing"><%= spot.category %></h4>
    <img src="https://kitt.lewagon.com/placeholder/users/krokrob" class="card-trip-user avatar-bordered" />
  </div>
  <% end %>
</div>
</div>

</div>
